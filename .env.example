# Google Analytics 4 Measurement ID (G-XXXXXXXXXX).
# Leave empty to disable analytics + cookie banner entirely (default for local dev).
NEXT_PUBLIC_GA_ID=

# Optional: Google Search Console HTML meta-tag verification token.
# Preferred method is a DNS TXT record; this is a fallback.
GSC_VERIFICATION=
